// ============================================================
// 站点唯一真相源 —— 价格 / 会员规模等会变动的事实集中在此
// 改价、改措辞只改这里，全站自动更新（避免多处不一致）
// ============================================================

/** 价格：实际计费 = 每 PRICE.months 个月一次性收 PRICE.total；perMonth 仅为展示锚点 */
export const PRICE = {
  symbol: '$',
  perMonth: 60,   // 展示用「每月约」
  total: 180,     // 实际每期结算额
  months: 3,      // 计费周期（个月）
};

/** 会员规模措辞 —— 不写死具体数字（会增减），随规模变化无需改文案 */
export const MEMBERS = {
  zh: '数百位',
  en: 'hundreds of',
};
