---
export const prerender = false;
import Base from '../../layouts/Base.astro';
import { verifyToken } from '../../lib/apply-token';
import { supabaseServer } from '../../lib/supabase';

function env(key: string): string | undefined {
  return (Astro.locals as any)?.runtime?.env?.[key] ?? (import.meta.env as any)[key];
}
const esc = (s: string) => s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');

const url = Astro.url;
let state: 'ok' | 'invalid' | 'error' = url.searchParams.get('ok') === '1' ? 'ok' : 'error';

const token = url.searchParams.get('token');
if (token && state !== 'ok') {
  const key = env('RESEND_API_KEY');
  const secret = env('APPLY_SECRET') || key || '';
  const payload = await verifyToken(token, secret);
  if (!payload || payload.p !== 'confirm' || !payload.id) {
    state = 'invalid';
  } else {
    const supabase = supabaseServer(Astro.cookies, Astro.request.headers);
    const { data: rows, error } = await supabase.rpc('confirm_web_application', { p_id: payload.id });
    const row = Array.isArray(rows) && rows.length ? rows[0] : null;
    if (error) {
      state = 'error';
    } else if (!row) {
      state = 'invalid';
    } else if (!row.newly_confirmed) {
      state = 'ok'; // 已确认过，不重复发信
    } else if (!key) {
      state = 'error';
    } else {
      // 首次确认：把这唯一一封申请送到 hello@
      const to = env('CONTACT_TO') || 'hello@sync-value.com';
      let when = '';
      try { when = new Intl.DateTimeFormat('zh-CN', { timeZone: 'Asia/Tokyo', dateStyle: 'medium', timeStyle: 'short' }).format(new Date()) + ' (JST)'; } catch {}
      const langTag = row.lang === 'en' ? ' (EN)' : '';
      const html = `<div style="font-family:-apple-system,'PingFang SC',sans-serif;font-size:14px;line-height:1.7;color:#232323;max-width:560px">
        <div style="background:#16314f;color:#fff;padding:14px 18px;border-radius:10px 10px 0 0;font-weight:600">Lighthouse Club · 入会申请${langTag}　<span style="color:#7fd6a3">✓ 邮箱已验证</span></div>
        <div style="border:1px solid #e6ebf1;border-top:0;border-radius:0 0 10px 10px;padding:18px">
          <table style="font-size:14px;margin:0 0 8px">
            <tr><td style="color:#6b7a88;padding:2px 12px 2px 0">称呼</td><td><strong>${esc(row.name)}</strong></td></tr>
            <tr><td style="color:#6b7a88;padding:2px 12px 2px 0">邮箱（已验证）</td><td><a href="mailto:${esc(row.email)}">${esc(row.email)}</a></td></tr>
            ${when ? `<tr><td style="color:#6b7a88;padding:2px 12px 2px 0">确认时间</td><td>${esc(when)}</td></tr>` : ''}
          </table>
          <p><strong>加入动机：</strong></p><p style="white-space:pre-wrap;border-left:3px solid #ccc;padding-left:12px;margin:4px 0 12px">${esc(row.motivation)}</p>
          ${row.source ? `<p><strong>来源：</strong>${esc(row.source)}</p>` : ''}
          <p style="color:#9aa7b4;font-size:12px;border-top:1px solid #eef2f6;padding-top:10px;margin-top:14px">申请人邮箱已通过点击验证链接确认，回信可直达（reply-to 已设为其邮箱）。</p>
        </div>
      </div>`;
      try {
        const res = await fetch('https://api.resend.com/emails', {
          method: 'POST',
          headers: { Authorization: `Bearer ${key}`, 'Content-Type': 'application/json' },
          body: JSON.stringify({ from: 'Lighthouse Club <noreply@mail.sync-value.com>', to: [to], reply_to: row.email, subject: `[入会申请·已验证]${langTag} ${row.name}`, html }),
        });
        if (res.ok) return Astro.redirect('/apply/confirmed?ok=1');
        state = 'error';
      } catch { state = 'error'; }
    }
  }
}
---
<Base title="邮箱已验证 — Lighthouse Club" description="Lighthouse Club 入会申请邮箱验证。" noindex={true} routeKey="apply">
  <div class="subpage"><div class="container"><div class="account-box" style="max-width:520px">
    {state === 'ok' && (
      <Fragment>
        <div style="font-size:1.8rem">✅</div>
        <h1 style="margin:.3rem 0">邮箱已验证，申请已提交！</h1>
        <p>手续全部完成。我们会认真阅读，并在 <strong>2 个工作日内</strong>把结果回复到你的邮箱，请留意收件箱（包括垃圾箱）。</p>
        <p class="fine" style="margin-top:16px"><a href="/">← 回到首页</a></p>
      </Fragment>
    )}
    {state === 'invalid' && (
      <Fragment>
        <div style="font-size:1.8rem">⏳</div>
        <h1 style="margin:.3rem 0">链接无效或已过期</h1>
        <p>这个验证链接无法识别，可能已超过 24 小时。请重新提交一次申请，我们会给你发新的验证邮件。</p>
        <p class="fine" style="margin-top:16px"><a class="btn gold" href="/apply/">重新申请</a></p>
      </Fragment>
    )}
    {state === 'error' && (
      <Fragment>
        <div style="font-size:1.8rem">😥</div>
        <h1 style="margin:.3rem 0">出了点问题</h1>
        <p>验证时遇到临时故障。请稍后重新点击验证邮件里的链接，或<a href="/contact/">联系我们</a>。</p>
      </Fragment>
    )}
  </div></div></div>
</Base>
